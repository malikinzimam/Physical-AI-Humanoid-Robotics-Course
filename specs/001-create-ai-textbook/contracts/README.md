# API Contracts

**Generated**: 2025-12-03

This feature is implemented as a local command-line script. It does not expose any public APIs (e.g., REST, GraphQL). Therefore, no external API contracts are required.

The script's internal "contract" is its command-line interface, which will be documented in `quickstart.md`.
